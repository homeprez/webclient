var app = angular.module('sampleApp');app.factory('DialogService',function ($http,$modal) {	return {		notify : function (myMessage) {			var modalInstance = $modal.open({				templateUrl: 'templates/dialog/Notification.html',				controller: 'ModalInstanceCtrl',				resolve: {					message: function() {						return myMessage;					}				}			});		},				error : function (myMessage) {			var modalInstance = $modal.open({				templateUrl: 'templates/dialog/Error.html',				controller: 'ModalInstanceCtrl',				resolve: {					message: function() {						return myMessage;					}				}							});		}	};});